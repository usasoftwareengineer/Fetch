package com.example.myapplication.data.viewdata

import com.example.myapplication.data.Model

/**
 * Represents the data that will be displayed in the user interface,
 * acting as a bridge between the domain models and the UI components.
 */
interface ViewData : Model